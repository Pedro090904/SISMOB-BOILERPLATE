apiVersion: v1
kind: Service
metadata:
  name: sismob-app-service
spec:
  type: NodePort
  selector:
    app: sismob-app-deployment
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30002
